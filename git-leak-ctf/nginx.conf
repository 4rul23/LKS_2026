server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Allow access to .git directory (the vulnerability!)
    location ~ /\.git {
        autoindex on;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
